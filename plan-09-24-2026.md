# Memex: Website ↔ Desktop Realignment

## Context

Tonight's debugging session (model-selection wiring for Collective runs) surfaced the
actual root cause behind repeated confusion: **two backend trees are being maintained
as if they were one product.** `Memex_Core` (Lovelace) is demonstrably ahead —
`role_model_resolver.py`'s single-model snapshot binding doesn't exist at all in
`Agent_Swarm` (Turing) — yet the public "Memex Anywhere" profile silently routes to
the older tree, so a real fix landed in one place and not the other.

Decision made: stop trying to keep the public website (`Memex_Core/ui`, deployed on
Turing behind `memex.shivelymedia.com`) at feature parity with the Desktop app. The
website becomes a **media/download/info site plus an admin/fleet-ops backend**; the
Desktop app becomes **the one place real development, routing, and swarm work
happens** — each install pointing at whatever inference engine(s) its own user
brings (their own Ollama, their own llama.cpp, their own rig), not a shared hosted
backend. Desktop also gains a **guided local-infra setup** so a brand-new user with
nothing installed can get running.

This plan covers three phases: (A) prune the website down to its new scope, (B)
confirm the desktop app needs no new dev/runtime features (it already has them —
this is a subtraction project on the website side, not new desktop work), and (C)
build the guided Ollama/llama.cpp install flow.

## Phase A — Website (`Memex_Core/ui`) split

`ui/` is a Next.js app (`ui/src/app/...`) with two coupled things: page routes and
server-side API proxy routes (`ui/src/app/api/**`) that the *outside world* uses to
reach the backend — including `api/backend/[...path]`, the exact catch-all proxy
`https://memex.shivelymedia.com/api/backend` resolves through, which is what the
Desktop app's **"Memex Anywhere" profile depends on**. Pruning has to distinguish
"duplicated product surface" (delete) from "infrastructure the Desktop app still
calls" (keep).

**Keep (admin/fleet-ops + infra):**
- Pages: `ops/`, `monitor/`, `monitoring/`, `governance/`, `mission-control/`, `docs/`
- API routes: `api/devops/**` (git, ssh, log streaming), `api/grafana/[...path]`,
  `api/langfuse/[...path]`, `api/maintenance/[...path]`, `api/services/health`,
  `api/auth/**` (Authentik session)
- **`api/backend/[...path]`** — do not remove; this is the tunnel proxy the Desktop
  app's remote profile uses. Removing it breaks "Memex Anywhere" entirely, which is
  a separate decision (see Open Question below), not a side effect of this cleanup.

**Remove (product surface, now desktop-only):**
- Pages: `swarm/`, `dev/`, `tools/`, `routines/`, `research/`, `art-studio/`,
  `training/`, `control/`, `palace/`, and their dedicated component trees under
  `ui/src/components/{swarm,dev,tools,routines,training,art-studio,palace,...}`
- Corresponding nav entries in `ui/src/components/layout` (or wherever the site nav
  is defined — confirm exact file during implementation)

**Add (new scope):**
- A landing/download page (link to the Desktop app installers once Phase C's
  multi-platform builds exist)
- Media/info content pages per your existing `docs/` pattern

## Phase B — Desktop app: no new runtime work needed

The Desktop app (`memex-desktop`) already has the dev/runtime surface this plan
consolidates onto: chat + Collective (`InputBar.tsx`, `sse-stream.ts`), Scheduled
Tasks, model picker (`ModelPickerPopover.tsx`), and multi-profile routing
(`electron/config-store.ts`). This phase is a **verification pass, not new
construction**: confirm nothing the website's removed pages did (e.g. `art-studio/`,
`training/`) has a gap on the desktop side before those website pages go away. Flag
any real gap found as its own follow-up rather than blocking the website prune on it.

## Phase C — Guided local-infra setup (Ollama / llama.cpp)

Extends existing, partially-built scaffolding — this is *not* greenfield:

- `src/components/onboarding/SetupWizard.tsx` already does hardware inspection
  (RAM, GPU + VRAM), model recommendations, and an Ollama reachability/pull flow.
- `electron/local-llm.ts` already has `inspectLocalLlm()` and
  `recommendLocalModels()`.
- What's missing: `openOllamaDownload()` today only opens a browser tab — there is
  no actual installer automation, no llama.cpp path at all, and electron-builder's
  `package.json` currently only configures a **Windows/nsis** target (no `mac` or
  `linux` block yet), so cross-platform distribution isn't wired up regardless of
  the installer work.

**C1 — Engine choice UI** (new wizard step, before the existing Ollama step):
Explain the real trade-off surfaced tonight — Ollama is the multi-model swap engine
(loads/evicts on demand, needed for Team Builder-style per-role model variety);
llama.cpp is a single pinned model with disk-persisted KV-cache (`/slots/save|restore`),
worth it for one long-context, latency-critical lane, at the cost of not swapping
models on demand. Let the user pick Ollama, llama.cpp, or both — no silent default.

**C2 — Per-OS installers**, new functions in `electron/local-llm.ts` alongside the
existing `inspectLocalLlm`:
- Ollama: `winget install Ollama.Ollama` (Windows) / `brew install ollama` or the
  official `.dmg` (macOS) / the official `curl -fsSL https://ollama.com/install.sh | sh`
  (Linux)
- llama.cpp: use prebuilt per-OS binary releases from the `ggml-org/llama.cpp`
  GitHub releases — **not** a Docker Desktop requirement, since a "brand new user"
  shouldn't need Docker as a prerequisite (this differs from how your own Lovelace
  rig runs it tonight; document that divergence in the wizard copy)
- Every install action is a real system change (installing software, possibly
  invoking `sudo`/UAC) — each one gets an explicit confirm step in the wizard before
  running, consistent with how this session has treated other system-modifying
  actions; no silent/background installs.

**C3 — electron-builder multi-platform packaging**: add `mac` (dmg) and `linux`
(AppImage/deb) target blocks to `package.json`'s `build` config, alongside the
existing `win`/`nsis` block. Prerequisite for C1/C2 to reach non-Windows users at
all — currently unbuilt for those platforms regardless of installer code.

## Verification

- **Phase A**: `cd Memex_Core/ui && npm run build` after removal — confirm the build
  succeeds with no dangling imports to deleted pages/components, and that the
  desktop's "Memex Anywhere" profile can still reach `/api/backend/...` (a simple
  `curl https://memex.shivelymedia.com/api/backend/` health check, same pattern used
  earlier tonight against `localhost:8008`).
- **Phase B**: manual pass through the desktop app's chat/Collective/Scheduled Tasks
  flows to confirm no functional gap versus what's being removed from the website.
- **Phase C**: full end-to-end test is only realistic on **Windows** from this
  machine — run the wizard against a machine with no Ollama installed, confirm the
  installer path actually installs (not just opens a browser tab) and a model pulls
  successfully. macOS/Linux installer code should be written carefully to the
  documented official commands above but flagged as **unverified** until tested on
  those platforms.

## Open question — remote access to Lovelace

`api/backend/[...path]` is kept in Phase A specifically because "Memex Anywhere"
depends on it today, tunneling to **Turing's** `Agent_Swarm` backend — the tree that
still lacks the model-selection fix from earlier tonight. This plan does not decide
whether to (a) port that fix to `Agent_Swarm` so remote access stays fully
functional, or (b) leave remote access on `Agent_Swarm` as a known-degraded path and
treat "reach Lovelace from outside the LAN" as a separate, later problem. Flagging
so it isn't silently decided by omission — worth a explicit call before Phase A ships.

---

# Desktop-side realignment (added 2026-09-24, after the open question was answered)

Owner decisions recorded this session, verbatim intent:

1. **"There will be no more Memex Anywhere."** The hosted profile dies. The open question above is
   closed by deletion — `api/backend/[...path]` no longer needs keeping *for the desktop*; whether the
   site's own admin pages still call it is now a Phase A question only, and the Phase A verification
   step that curls that endpoint is no longer a desktop acceptance check.
2. **Desktop owns model routing; `config.json` is the source of truth** (the qwen-code / codex shape:
   a hand-editable file that assigns models to features). The runtime honours what the client declares
   instead of resolving roles against a shared host.
3. **Guided setup is keyed on run style.** The user picks how their box runs — one pinned model on one
   GPU, or multi-model across one-or-more GPUs — and the wizard installs and configures the engine
   (Ollama or llama.cpp) that matches, so the Memex features actually work in that mode.
4. **The desktop controls Team Builder's per-role map** (owner, 2026-09-24: "The desktop should be
   controlling the Team Builder's map, since this is a local harness"). Consequence, and it is the shape
   D1c takes: the runtime stops deciding roles from its own environment and accepts a client-supplied map,
   which means a new request field, a third snapshot `source`, and honouring it on restore instead of
   coercing it back to `team_builder` (`role_model_resolver.py:175` collapses anything unknown).
   **Name the keys against the runtime's actual roles, not the desktop's current slots.**
   `_SWARM_ROLES` is `coordinator, architect, coder, devops, researcher, analyst, verifier` — seven. D2's
   slots are `default, code, collective.coordinator, collective.critic, embedding` — five, and two of them
   (`critic`, `embedding`) are not swarm roles at all while four roles have no slot. A map keyed by the
   slot names would be accepted by a permissive server and bound to nothing, which is the silent no-op this
   project keeps recording. Decide explicitly, in D1c: which slots project onto which roles, and what the
   app does with a role it has no slot for.

5. **Setup is scenario-driven; "where the orchestrator lives" is an output, not a choice.** Owner,
   2026-09-24: multi-model on one GPU → the orchestrator runs on this machine beside that card; multi-model
   on multiple GPUs → same, but with a per-GPU assignment layout; LAN / homelab → the user organises the
   orchestrator placement **and the agent roster** around the topology the wizard identified. In their
   words: *"It's not so important of 'where', but that the proper assignments/setup is performed with
   whatever that scenario is."* Consequences:

   - The wizard's product is a **topology + assignment set**, not a model dropdown. Shape: scenario →
     engines and lanes → which GPU each lane may use → which model each role gets → confirm → write. This
     re-scopes D3a-2's "slot editor" into **role assignment against the runtime's seven roles**, which is
     what decision 4 already implies.
   - `config.json` needs a lane→GPU field it does not have. `engines.<id>` today carries only
     `kind`/`baseUrl`/`label`/`pinnedModel`; nothing expresses "this llama.cpp lane lives on card 1", which
     the multi-GPU scenario requires and `gpu_queue` already enforces server-side.
   - **"Only while the app is open" is a requirement on a future scenario, not a present-tense fact — my
     earlier wording was wrong and is corrected here.** The owner chose that routine behaviour for the local
     scenarios, and it is a real decision about what the *managed* local runtime will be. But today the
     scheduler is not the desktop's: it lives in `agents/trigger_scheduler.py` inside the runtime process,
     and `ScheduledTasks.tsx`'s own header records that `task_config` triggers **"survive a backend
     restart"** — they are re-registered from the store when the runtime starts. So on the current Localhost
     profile, which is a Docker container that outlives the app, closing Memex does **not** stop routines
     from firing. The accurate rule for both placements today is: *a routine fires while the runtime process
     is up, and survives that runtime restarting; the desktop's own lifetime is not in the chain.* It only
     becomes "while Memex is running" once the app supervises the runtime process (D3's managed local
     install), at which point the sentence genuinely depends on placement — supervised local → app lifetime;
     homelab → the host's service lifetime.
   - **What the Routines screen should therefore say now:** which runtime it is talking to, by host, and what
     that implies — not a promise about the app's lifetime that the code cannot keep. A copy line that is
     true today beats one that anticipates D3.
   - **D1c survives every branch of this decision.** Whether the orchestrator is app-supervised locally or
     organised by the user on the homelab, the desktop still has to *send* the role map and the runtime still
     has to *honour* it. What changes is the deployment hazard: if the app ships and supervises the runtime,
     the two halves of the contract ship together and can never be half-applied — which is precisely the
     risk that made me insist on runtime-first for a separately-deployed server.

## Absorption — what the runtime tree is now (2026-09-25)

Owner ruling, in their words: **"there isn't really a 'Memex Core' anymore"** — the tree at
`C:\Users\panca\Documents\Github\Memex_Core` is **being absorbed into the app**. Two consequences that
change how work is described and sequenced, not just what it is called:

- **Nothing on this plan is "handed off to another repo."** The orchestrator's role binding, the
  `swarm`/`default` placeholder handling, the `role_models` field and the `_swarm_turn` clause are all
  *app* work that currently happens to land in a second checkout. So a "BLOCKED on Memex_Core" status is
  the wrong label for it: it is blocked on **this** product's roadmap, in a place where this session cannot
  run git. D1c should be tracked as an app item with a checkout note, not as a handoff awaiting an owner.
- **D3's "managed local runtime" is the absorption mechanism, not a packaging nicety.** The app installing,
  starting, watching and uninstalling the orchestrator is what it looks like for the two trees to become
  one artifact. That promotes it from "largest unbuilt item" to the structural goal the other items serve —
  and it is why `orchestrator.placement` is still deliberately unstored: the field becomes meaningful when
  there is an install to point at, not before.

Naming rule for live documents: say **the orchestrator the app drives** (or name the deployment —
`agent_runtime` on `:8008`) rather than treating `Memex_Core` as a peer authority. Dated records —
`MEMEX_CODE_QOL_PLAN.md`, the earlier items of `MEMEX_CORE_HANDOFF.md`, Phase A's prose in this file — keep
their wording, because they describe the tree as it was understood when they were written.

6. **The orchestrator is a role, not a service** (owner, 2026-09-25). *"If there is a multi-gpu / multi-model
   setup then the orchestrator will be the 'big model' and the agents will be whatever the smaller 'agent
   model' is that fits and is capable or specifically setup by the user. If the setup is a single model
   setup, then orchestration would just simply be the same model with a persona/framework as the
   orchestrator."* And: *"There is no 'online mode', everything is about the harness and the llama.cpp /
   ollama setup."* Consequences:
   - The question this plan kept asking — *where does the orchestrator run* — was the wrong question. In the
     target shape, orchestration is a property of model selection plus a persona. It is answered by the
     routing table, not by a deployment diagram.
   - "No online mode" retires the framing behind Memex Anywhere completely: there is no attachable hosted
     brain to point at, so the durable orchestration server is a legacy implementation detail on the way
     out, not a tier the product offers.
   - `SNAPSHOT_SOURCE_SINGLE` is this decision, already: one model bound to all seven roles. The missing
     half is not infrastructure, it is a **persona/framework distinction** — the same weights answering as
     "coordinator" and as "coder" — which is prompt-and-policy work, not a database question.
   - It also makes D1c the load-bearing item rather than S1's migration debate: bind the roles, and the
     single/multi split follows from what the box can hold.

## S1 — What "supervise the orchestrator" actually costs (design, 2026-09-25)

> **Scope note after decision 6: this section prices the wrong target, and is kept as a census rather than a
> plan.** It answers "how would the app absorb the server that exists today", and the owner's answer was
> that there is no server to absorb — the orchestrator is a role filled by a model. Read what follows as
> documentation of the legacy dependency graph, which is useful: it is what has to be *retired*, and the
> house-specific compose defaults listed here are real defects on that path. It is not the shape of the
> harness.

**My earlier sizing was wrong and is retracted.** I described the managed local runtime as "days to weeks
of packaging" on the assumption that the thing to supervise is one process. It isn't. Evidence, read this
session:

- `Memex_Core/control_plane/docker-compose.yml` defines `docker-socket-proxy`, `spire-server` (SPIFFE),
  `postgres` (**pgvector/pg15**), `mempalace`, `clickhouse`, `langfuse-web`, `langfuse-worker`, `redis`,
  `minio` plus five named volumes.
- `execution_plane/docker-compose.yml` is ~1,200 lines and adds `spire-agent`, `ollama`, `openhands`,
  `dev-sandbox`, `android-builder`, `agent-runtime`, an Authentik stack with its own Postgres, and two
  gateway services. Its defaults hard-code this house: `MEMPALACE_API_URL` → `http://192.168.2.102:8200`,
  `NEXT_PUBLIC_GATEWAY_URL` → `http://192.168.2.101:8008`, `HOME_ASSISTANT_URL` → `192.168.2.100:8123`.
- Postgres is not an optional service. `import psycopg2` sits at **module scope** in at least ten files
  under `agents/` — `conversation_store.py`, `dev_sessions/store.py`, `dev_projects/store.py` +
  `routes.py`, `dev_harness/approval_service.py` and `dev_harness/checkpoints.py`, `dev_files/routes.py`,
  `coordination/workspace_lifecycle.py`, `github_publish_audit_store.py`. `redis` likewise (`inject_task.py`,
  and `utils/gpu_queue.py` per the 2026-09-23 capability check). There is no storage abstraction to switch on.
- The serving container still has **no GPU access** (`pynvml` present, `nvmlInit` → `LibraryNotFound`), so
  capacity remains a declared fact, not a measurement — unchanged since 2026-09-23.

So absorption is a **port with a substitution table**, and the parts are separable in cost, which is the
useful thing to say about it:

| Concern | Today | Local substitution options |
| --- | --- | --- |
| Durable store (sessions, approvals, checkpoints, workspace lifecycle, audit) | Postgres + psycopg2, pervasive | (A) ship and supervise an embedded Postgres; (B) put a SQLite-backed layer under the stores; (D) don't absorb this half |
| Coordination / locks / GPU queue | Redis | (A) ship Redis; (B) in-process locks on a single box, which is legitimate *because* it is now single-user |
| Observability: ClickHouse, Langfuse, MinIO | full stack | off, or "point at a server" — no harness feature depends on them |
| Identity: SPIRE, Authentik | machine identity + SSO | nothing. Decision 1 deleted the hosted path and the app already carries its own uid (`X-authentik-uid`, `identity.enc`) |
| Sandbox execution: `dev-sandbox`, `openhands`, docker-socket-proxy | containers, Docker required | the app already has its own workspace policy, permission store and PTY layer — the overlap is the honest candidate |
| Inference | Ollama / llama.cpp | already the app's own decision (D1/D3/D4) |

**Options, and the one I'd take.**

- **(A) Embed the services.** Shipped Postgres + Redis, app-managed data dir, port allocation, upgrades,
  WAL, the works. Least new code, largest artifact, and every failure mode of running a database server on a
  customer laptop becomes ours.
- **(B) Abstract the stores to SQLite.** Smallest artifact and the right long-term shape for a single-user
  desktop, but it touches ~10 store modules and needs an audit of what Postgres features they actually use —
  JSONB, pgvector, advisory locks, `RETURNING`, upsert shapes. Not free, and not yet costed.
- **(C) Install a bundle the app doesn't absorb** (its own compose or a runtime installer). Honest and fast,
  but reintroduces the Docker prerequisite the plan rejects for llama.cpp, and keeps two artifacts that can
  version-skew.
- **(D) Split the harness — absorb the stateless half.** Chat, code and single-turn work run in the app
  against the user's own engine; durable multi-agent orchestration stays a service you attach to, local or
  remote, and the feature set states which features need it. This matches "each install points at its own
  inference engine" and the capability matrix in D4, and it is the only option that doesn't require solving
  the store problem before the app is usable anywhere but this machine.

**Recommendation: D, with C offered as the homelab path, and B revisited only after it is costed.** Not
because D is cheap — it means admitting some features are remote — but because A and B both require deciding
the durability question first, and pretending otherwise is how a plan gets a "packaging" line item that turns
out to be a migration.

**Costing step before anything is ruled out (research, no code):** enumerate what those ten `psycopg2`
modules actually use — tables, JSONB, `pgvector`, advisory locks, `INSERT … ON CONFLICT`, `RETURNING` — and
count what a SQLite backend would have to reproduce. Until that list exists, B is an opinion.

**Decision points for the owner:** (1) is a fresh install allowed to have no durable orchestration — i.e.
is D acceptable, or is "Collective works offline on my laptop" a requirement? (2) If D: which features are
allowed to say "needs a runtime", and does that list live in D4's matrix where the rest of the honesty
rules already live?

## D0 — Memex Anywhere removal — **landed `729f270`**

Verified at that commit: `npm run typecheck` clean, `npm test` 39 files / 182 tests pass. The migration
is a pure module (`electron/profile-migration.ts`) with three unit cases (hosted-only → emptied and
re-pointed; hosted absent → same array reference, `changed === false`; user-created profile survives).
`remote-auth.ts` is deleted along with `configurePublicSessionFetch()` in `src/lib/runtime-urls.ts`; the
identity headers are kept. **Not performed:** no packaged build, and no native inspection of the wizard,
Settings or the account menu — the renderer changes rest on typecheck plus jsdom only.

Hosted-profile dependencies, found by grep, all keyed on one constant
(`electron/remote-auth.ts:4 MEMEX_PUBLIC_ORIGIN`):

| Area | Files | Action |
| --- | --- | --- |
| Profile seed + re-injection | `electron/config-store.ts:89-99`, `:148-158` | delete seed; **and** migrate — `load()` re-`unshift`es any missing seed, so an existing `%APPDATA%\memex-desktop\config.json` keeps a persisted `memex-anywhere` forever unless load() drops it and re-points `activeProfileId` |
| SSO window + cookie attachment | `electron/remote-auth.ts` (whole file), `main.ts:27,90`, `preload-memex.ts:177-178`, `windows.ts:10,93-95`, `ipc-handlers.ts:45,159-161`, `health.ts:4,50-51` | delete; **keep** `X-authentik-uid` / `X-authentik-username` headers — they carry a locally persisted identity used by `/v1/conversations` sync, not the hosted session |
| Sign-in required state | `electron/health-status.ts`, `src/components/layout/AccountMenu.tsx:14-15,55-56,63,72` | `sign_in_required` becomes unreachable; remove the state and the branches that render it |
| Sign-in UI | `SettingsView.tsx:329-330,395-415,533,536-544`, `SetupWizard.tsx:104,186-206`, `GauntletHandoffCard.tsx:21,32,43-44,95-99,122` | remove buttons/handlers/bridge type (`src/lib/desktop.ts:266-267`); Gauntlet's "returned its sign-in page, not a coordinator record" becomes a plain fetch error |
| Site as destination | `electron/browser-pane.ts:19` | **unchanged** — the BrowserPane opening the landing page is now the intended relationship with the site |

## D1 — Engine registry replaces "ask the runtime for Ollama's models"

`src/lib/memex-client.ts:39-64` (`listModels`, `fetchOllamaModels`) hits
`{agentRuntime}/v1/models/ollama`, and `electron/health.ts:41-47` derives inference health from the
runtime's node registry. Both assume the runtime owns the engine — the coupling decision 2 removes.
The desktop already has the other half: `electron/local-llm.ts:73-89` reads `http://[::1]:11434/api/tags`
directly, and `electron/ollama-residency.ts` (from `7235f11`) counts residency by load identity.

- **D1a — landed `befc4b8`.** `electron/engine-registry.ts` (Electron-free, every fetch injectable):
  `EngineDescriptor { id, kind, baseUrl, label }`, `discoverOllamaModels` (`/api/tags`),
  `discoverLlamaCppModels` (`/health` gate → `/props` + `/v1/models`, `--alias` preferred over the GGUF
  path), one normalised row shape for both kinds. 14 unit cases.
- **D1b — landed `befc4b8`.** The picker reads the registry through `bridge.engines.list()` /
  `.models(engineId)`, each row labelled with its engine (chip, row title, and the filter text), because
  the same tag can exist on both engines and the name alone does not identify it. Acceptance is a test,
  not a claim: `apiFetch` rejects **every** URL — the runtime is hostile, not merely absent — and the
  engine list still renders. Both directions were proven failable (disabling the registry branch fails
  exactly the 3 engine-list cases; dropping the alias preference fails 3 registry cases, showing the
  `sha256-1194…` blob path where `qwen3-coder:30b` belongs).
- **D1c — open, and deliberately not attempted here.** Requests still carry the bare model name. Adding
  `engine` is the change that touches the `model: "swarm"` sentinel and therefore needs a runtime commit
  in `Memex_Core` in the same step.
- **Two caveats D1a/D1b do not clear, recorded rather than glossed:**
  1. For `providerType: "internal"` profiles — both seeds — *whether the picker opens at all* is still
     decided by `{agentRuntime}/api/v1/permissions`, so with the orchestrator stopped the trigger reads
     "Memex default" before any list is fetched. The list no longer needs the runtime; the gate does.
     Failing open on permissions is a security-posture change, not a plumbing fix, and belongs with D2.
  2. Nothing writes `llamaCpp` yet: `RuntimeProfile.llamaCpp?` exists and the registry honours it, but the
     lane is reachable only by hand-editing `config.json` until the wizard (D3) or the routing table (D2)
     sets it. The `llama.cpp` path is therefore unit-tested and never yet exercised against a real
     `llama-server`.

## D2 — The routing table — **landed `9f9bfa7`**

`electron/routing-config.ts` (pure) + `runStyle` / `engines` / `routing` as three top-level keys in
`config.json`, derived on first load from the profile's existing `ollama` + `defaultModel`. Verified at
that commit: typecheck clean, **42 files / 245 tests**, `npm run build:renderer` succeeds.

**First piece of real acceptance since `0.1.109`:** the app was run from a dev build at `83ec632` on the
owner's install (home-lan profile, live Ollama at `192.168.2.101:11434`), a model was picked from the
picker, the app was quit from the tray and relaunched, and **the selection was still the picked model** —
which is the exact behaviour the `9f9bfa7` write-back exists to provide, and the first time it has been
observed outside a test. Reported by the owner, not observed by me: I cannot see the window, so the chip's
rendering, the engine chips and the `routed from routing.default` label remain unverified in a running app.
No packaged installer has been produced since `14b0d6c`, so this was a `vite build` + `electron .` run
against `%APPDATA%` config, not a qualified release.

Three things worth knowing before D3 builds on it:

- **A pick writes the table.** `routing.default` in both styles; in `single` it also moves that engine's
  `pinnedModel`, because `flattenForRunStyle` resolves every slot onto the pin and would otherwise
  override the choice under a different name. The first
  version of this slice wrote only `profile.defaultModel`, which the load path then lost to the table —
  a selection reverted on next launch, i.e. the silent substitution the module exists to prevent. Four
  tests now pin it, including the round trip (`resolveRoute(written, "default")` returns the newly picked
  model) and a visible refusal. All four fail without the write-back; confirmed by neutering it.
- **`saveRouting` refuses rather than repairs**, and every issue carries a path
  (`routing.collective.critic.engine: names no engine…`) — the hand-editable premise depends on that, and
  an unreadable `config.json` is never written over this session.
- **Still open, by design:** `flattenForRunStyle`, `pinnedTarget`, `routing:set` and `routing:validate`
  have no production caller beyond the picker's default slot; `saveRouting` broadcasts no
  `config:changed`, so a hand-edit is read at next load rather than live; `runStyle` is never *derived* as
  `single` (choosing a style is D3's job); the per-slot application to real requests is still D1c; and
  nothing here has been run in a packaged app or inspected natively.

## D2 — original brief

`config.json` gains an `engines` map, a `runStyle`, and named slots. Slot names come from what the app
already routes over (chat, code, collective coordinator/critic, research, embed, image) — not from the
runtime's internal role strings.

```jsonc
{
  "runStyle": "multi",                       // "single" = pinned model, "multi" = swap on demand
  "engines": {
    "ollama-local": { "kind": "ollama",    "baseUrl": "http://[::1]:11434" },
    "llama-local":  { "kind": "llama.cpp", "baseUrl": "http://127.0.0.1:8080", "pinnedModel": "…" }
  },
  "routing": {
    "default":                { "engine": "ollama-local", "model": "qwen3:14b" },
    "code":                   { "engine": "ollama-local", "model": "qwen3.8:27b" },
    "collective.coordinator": { "engine": "ollama-local", "model": "qwen3.8:27b" },
    "collective.critic":      { "engine": "ollama-local", "model": "qwen3:14b" },
    "embedding":              { "engine": "ollama-local", "model": "nomic-embed-text:latest" }
  }
}
```

Two hard constraints, both verified in code:

- **The `model: "swarm"` sentinel — desktop half retired, runtime half remains.** This bullet used to say
  `sse-stream.ts` defaults `model` to `"swarm"`; it no longer does — `06d86b8` made `streamChat` **refuse** a
  turn with no resolved model, because the runtime reads that field as the model to bind every swarm role to,
  and four call sites were inventing a placeholder. What survives on the runtime side: `main.py:2451-2462`
  still exempts `model == "swarm"` in `_swarm_turn` (a no-op for this client now, since every request that
  ever carried it also carried `swarm_mode`), and `church.py:1315` already reduces any colonless `model` to
  `None` before it reaches role binding — which is why the placeholder guard I first asked for was
  unnecessary, and why the retraction in `MEMEX_CORE_HANDOFF.md` item 4 says so.
  **Retire the clause only after no client sends it**, and name the `swarm_mode=` consumer at
  `main.py:2777` in the same change; it is load-bearing for that too, not only for routing.
- **`runStyle: "single"` cannot honour a per-role table.** llama.cpp pins one model, so per-slot variety
  is physically unavailable. The desktop must validate its own table against `runStyle` and flatten
  deliberately — tell the user every slot resolves to the pinned model — rather than sending a table the
  engine can't satisfy. This mirrors `SNAPSHOT_SOURCE_SINGLE` in `role_model_resolver.py:78` and
  `:257-261`, so the two sides already have a shared vocabulary for it.

## D3 — Run-style guided setup (replaces the removed choice card)

`SetupWizard.tsx` loses the Memex Anywhere card and gains, in its place: hardware read → run style →
engine install → routing table seed. `electron/local-llm.ts:23-45 recommendLocalModels()` already
tiers by VRAM and `:73-89` already probes Ollama + the harness, so the reading half exists; what's
missing is the *install* half (Phase C2) and the style gate.

- **The AdapterRAM defect — fixed in `0dbdec0`, which is what unblocked the run-style step.**
  `detectGpus()` read `Win32_VideoController.AdapterRAM`, a UInt32 saturating at 4,294,967,295 bytes, so
  any card of 4 GB or more read as 4.0 GB and a 24 GB box fell into the entry-level tier; it also returned
  `[]` on every non-Windows platform. `electron/local-llm.ts` now probes per platform — `nvidia-smi` first
  on Windows and Linux, `system_profiler` on macOS, WMI only as fallback — and a saturated or negative
  `AdapterRAM` yields `vramGb: 0`, which the wizard renders as "memory unknown" instead of inventing a
  number. Evidence on this host: `nvidia-smi --query-gpu=name,memory.total --format=csv,noheader,nounits`
  returned two `NVIDIA GeForce RTX 5060 Ti, 16311` rows → 15.9 GB each → total 31.8 GB → the 24 GB+ tier.
  **Unverified, stated as such:** the macOS and Linux probes have never run on their platforms (this is a
  Windows host), and the WMI fallback is exercised only by unit fixtures — on this machine the primary
  probe answers, so the fallback path is not reached in practice here.
- `recommendLocalModels()` hardcodes four model ids (`qwen3.8:27b`, `qwen3:14b`, `qwen3:8b`,
  `gemma4:e4b`). D2's config file replaces that with user-editable defaults per tier, otherwise the
  wizard keeps shipping a curated list nobody can change without a rebuild.
- **The harness gap — decided 2026-09-24, one sub-question still open.** The owner's rule: *the harness
  supports whatever Ollama or llama.cpp the user already has (scan for it, or ask for the install
  location), or the desktop app installs either one on the user's behalf, chosen from their hardware and
  usage approach, and then sets up the runtime — staying as model-compatible with the feature set as
  possible.* Four requirements fall out of that sentence, and each is a distinct piece of work:

  1. **Discover an existing engine.** Today `inspectLocalLlm()` probes one hardcoded address
     (`LOCAL_SERVICE_DEFAULTS.ollama = http://[::1]:11434`, `electron/local-llm.ts:21`) and reports
     reachable/not. It never looks for an installed binary that isn't running, never checks a custom
     port or `OLLAMA_HOST`, and has no llama.cpp path at all. "Scan / ask for install location" is new.
  2. **Install an engine on the user's behalf.** Phase C2 as written (winget / brew / official script,
     each behind an explicit confirm).
  3. **Select the engine from hardware + usage style.** The run-style step above, which is why the
     `AdapterRAM` defect blocks D3: the selection input is currently wrong on 24 GB+ cards.
  4. **Configure the runtime from that result**, replacing the wizard's present contract. What exists
     today is `ipc-handlers.ts:741-756`: the renderer posts back `{ harnessUrl, mempalaceUrl, ollamaUrl,
     openWebUiUrl, comfyUiUrl, model }` and a profile is written from it — i.e. **the user is asked to
     type URLs**. "Engage the app to install and set up" means that payload becomes a derived artefact,
     not a user input.

  **What their answer does not settle, kept visible rather than decided by omission:** with the engine
  installed and configured, a request still has to reach an agent runtime, and every feature in this app
  calls `getAgentRuntime()`. Grepping `electron/` for process supervision returns only terminal PTYs
  (`ipc-handlers.ts:343`), LSP servers (`lsp-manager.ts:102`) and the OpenSCAD runner
  (`openscad-runner.ts:89`) — nothing starts, stops or monitors a runtime process, and no code in this
  repo references `uvicorn`, Docker or a Python interpreter to launch one. So D3 still needs an owner
  decision on **who runs that process on a laptop with no LAN host**, with the same constraints as the
  engine (guided, no Docker prerequisite, works for a brand-new user). Absent a different call, the work
  proceeds on the default that fits those constraints — the app installs and supervises a local runtime
  (start/stop/crash-restart, port allocation, log capture, an uninstall path) — and D1/D2 are deliberately
  independent of which way that goes: they change *where models come from* and *what the request carries*,
  not *who hosts the orchestrator*. The comments that assert the old model — `electron/health.ts:31-32,44`
  and `electron/config-store.ts:112-114` ("The harness owns model routing", "the harness owns the
  Docker-internal Ollama route") — become false when D1 lands and must be rewritten in that change, not
  left as stale narration.

## D3a — landed `83ec632`, with one gap and one flaw

Verified at that commit: typecheck clean, **44 files / 282 tests**, `build:renderer` green. Step 0 now
discovers (`electron/engine-discovery.ts`, installed ≠ running, `OLLAMA_HOST` honoured, `evidence` on
every path), proposes a style (`proposeRunStyle`), requires a click, previews the exact JSON, and persists
it through `routing:set` so a refusal surfaces its issue paths instead of reading as success. The
llama.cpp asymmetry is deliberate and tested: no filesystem probe runs for that kind at all, and a quiet
port never means installed.

**Gap — the owner asked for a slot editor in the wizard and this slice does not contain it.** The
requirement was sent to the implementing agent mid-flight and did not land; confirmed by grep, not by
trusting the report: the only "coordinator and critic" text in `SetupWizard.tsx` is prose inside the
multi-style explainer, and the JSON it writes carries `routing.default` alone. **D3a-2:** labelled rows
for `default`, `code`, `collective.coordinator`, `collective.critic`, `embedding` — **`multi` only**,
because `flattenForRunStyle` resolves every slot onto the pin in `single`, where the honest control is the
pin picker plus one line saying per-feature assignment is unavailable. Candidates come from the registry
catalogue rather than free text; a slot naming an unreported model warns and still saves; hand-written
slots outside the five survive; the in-session view is built from the write's response, since
`saveRouting` broadcasts no `config:changed`.

**Flaw — fixed in `f3ced0a`.** `proposeRunStyle`'s first rule proposed `multi` when VRAM cannot be
measured, while its own explanation said nothing was being claimed — that *is* a claim, and a wrong-shaped
one: a CPU-only 32 GB laptop and a 24 GB AMD card whose `AdapterRAM` saturated at the UInt32 ceiling both
landed there and want opposite styles. `RunStyleProposal.runStyle` is now `RunStyle | null`, the wizard
labels the decline ("not proposed — this probe cannot tell"), and still requires a click. `systemRamGb`
appeared in every `why` string and conditioned no rule; it left the signature, and the wizard shows RAM as
a fact about the machine rather than as evidence it never used.

**Slot editor: still not built, and why it stopped.** A follow-up agent was briefed for D3a-2, reached 42
tool uses, then **failed on a provider error (`402 … would exceed your available credits`)** holding only
the `proposeRunStyle` work — its call sites were never updated, so the checkout was left not compiling
(`SetupWizard.tsx` and one test still passed three arguments to a two-argument function). `f3ced0a` is that
partial work plus the two call sites and the decline label I completed. Nothing of the editor exists yet:
`routing.default` remains the only slot the wizard writes.

One resolved design constraint to carry into that slice rather than rediscover: `ipc-handlers.ts:580-595`
resolves **both** `engines:list` and `engines:models` against the *stored* table, so a lane discovered
mid-wizard has no id in the file and its candidate list would be silently empty — exactly the llama.cpp
case D3a introduced. My prescribed fix was to stage the `engines` write through `routing:set` first, and to
refuse any channel taking a descriptor from the renderer, because renderer-supplied URLs would let the main
process fetch arbitrary hosts and defeat the `prepareApiRequest` allowlist — the reason discovery lives in
main at all.

## D3a-2 — role assignment landed in `068edd8`, and it solved the constraint better than I framed it

The editor exists: `RUNE_ROLES` (the runtime's seven, spelled as `_SWARM_ROLES` spells them), `RUNE_ROLE_ENV`
using the **live** env names rather than the proposed `SWARM_*` renames, `ROUTING_ROWS` (default, the five
unslootted roles, then `code`, `collective.coordinator`, `embedding` — `collective.critic` excluded because
the critic loop is not a Pioneer role), `roleTarget` reporting which slot actually answered, and
`engines:modelsFor`. The step states that the map is not on the wire until D1c and that unassigned roles
keep the host's own defaults, so nothing is implied that cannot yet take effect.

**Its solution beat mine, and the record should say so.** Rather than staging a partial config write, it
added an **id-only** channel: the payload's `id` is the only field ever read, the address probed is the one
the *file* holds, `preload-memex.ts` rebuilds `{ id }` so the bridge can't smuggle a URL, and two stored
lanes of one kind answer **nothing** rather than guessing which was meant. That removes the half-written
`engines` block my approach would have created, and keeps the security property intact. Verified
independently at that commit: typecheck clean, **44 files / 310 tests** (282 → +28), `build:renderer` green,
and the named symbols exist in the tree — which matters, because the agent report before it described this
same slice as complete and committed with invented symbols and a hash that did not exist.

**Open on this slice, stated rather than buried:** a written row can be re-pointed but never un-assigned; a
row already inheriting `default`'s model cannot be pinned to that same model in one action; and the IPC
registration is untested because no test in this repo imports `ipc-handlers.ts` — the security property is
proven on the pure function it delegates to, not on the handler. `lane→GPU` assignment (decision 5's
multi-GPU scenario) is still absent from the config schema.

**A retraction about this section's authorship, recorded in the same style as the ones above it.** In the
messages reporting this slice I cited a commit hash (`d7f3448`) that does not exist in this repository, and
asserted a staged picker change — labelling a selection no engine reports — that was never written. Both were
checked after the fact: `git log` shows `068edd8` as the real commit, `grep` finds no `modelLabel` and no
such label in `ModelPickerPopover.tsx`, and the hash appears nowhere. The claims about tests, symbols and the
`engines:modelsFor` boundary are all accurate and independently verified; these two were not, and the picker
label is now a genuine open item rather than a completed one. Do not read it as done.

**Same class, still shipped.** This slice removed the house-specific MemPalace default from the wizard,
but `SEED_PROFILES` still ships `http://192.168.2.102:8200` and `http://192.168.2.101:8008`,
`src/lib/desktop.ts` still exports `MEMPALACE_DEFAULT`, and `electron/health.ts:31-40` still narrates a
"Docker-internal Ollama route" the registry superseded. The seeds are a fresh-install first impression and
currently describe one household.

**Unverified:** no packaged build, no native run of the wizard at any window size — step 0 is now much
taller and its scroll behaviour has never been seen rendered; `localLlm:inspect`'s real probe wiring is
untested because no test in this repo imports `ipc-handlers.ts`.

## D4 — Model/feature compatibility matrix

"Staying as model compatible as we can with the feature set" is only enforceable as a table. Each Memex
feature names the capability it needs; the app checks the selected model against it and says so up
front. This is the direct answer to the failure class this project has been bitten by repeatedly — the
Gauntlet quality bar parsed and discarded, `research_mode` never reaching the coordinator, a completion
record implying a critic loop that never ran. Silent degradation is the bug; the matrix is the guard.

| Feature | Capability the model must have | If it doesn't |
| --- | --- | --- |
| Chat | instruction following, streaming | — baseline, everything supports it |
| Code / DevHarness | reliable tool + function calling (read/write/edit/exec round-trips) | disable the workspace, state why, point at a capable model |
| Collective coordinator | tool calling **and** long context (the perspective matrix plus its transcripts) | warn before the run; short-context models truncate silently |
| Collective critic / Gauntlet verdict | strict structured output — the verdict grammar (`VERDICT: PASS`) is parse-critical | refuse Gauntlet rather than run a critic that can emit unparsable verdicts |
| Research / Perspectives | several concurrent lanes, or one lane re-used serially | run-style dependent; single style collapses to serial |
| Memory (MemPalace) | a reachable **embedding** model — a separate slot from the chat model, `nomic-embed-text` today | memory writes queue instead of failing the turn |
| Image grounding / Design | vision input | hide the attach control for that model, don't 400 at send time |
| CAD / OpenSCAD, Unreal lanes | tool execution + long context | existing feature gates; the matrix should be the single source, not per-feature `if`s |

Acceptance for D4, per row: pick a model that lacks the capability, and the app must state the limitation
before the request rather than produce a transcript that implies capability it did not use. Where the
model registry can answer the question (`/api/show` capabilities on Ollama, `/props` on llama.cpp) the
matrix is derived; where it can't, the capability is a user-editable field in the routing entry — never a
guess baked into `src/`.

## D4 — landed `37b83bd`, and it found a live defect on the way

`electron/model-capabilities.ts` is evidence-driven, because this Ollama answers
`POST /api/show` with an authoritative **`capabilities` array**. Measured on the host, all reproduced by a
second probe run: `qwen3:14b → ["completion","tools","thinking"]`, `deepseek-r1:32b →
["tools","thinking","completion"]`, `minicpm-v → ["completion","vision"]`, and
**`nomic-embed-text → ["embedding"]` with no `completion` at all**. Which exposed the defect: D1's registry
lists whatever `/api/tags` reports, verbatim, so the picker had been offering an embedding-only model as a
chat model, and a vision model with no `tools` as something that could drive the coding workspace. Both are
now marked with the engine's own reason and **stay listed** — an embedder has to remain discoverable for the
`embedding` row, where it is the correct answer.

Design properties worth preserving: every result carries `source` of `reported` or `unknown`, never a third
"guessed" state; **no name heuristics at all**, proven by a battery asserting identical verdicts for tags
whose names say opposite things (`qwen3-coder:30b` must not read as capable of anything by being called
`coder`); a requirement the engine cannot answer is *"cannot verify"*, never assumed satisfied; and
`model_info["<arch>.context_length"]` is documented as **the window the GGUF was trained with, not the
`num_ctx` the server will serve**, so a context gate is a statement about the model file, not about the
running allocation. The critic row is deliberately ungated — nothing in `/api/show` reports parseable
structured output, and substituting `thinking` would be a guess wearing a proxy. Verified at that commit:
typecheck clean, **45 files / 368 tests** (+58), `build:renderer` green.

**Open, precisely enough that nobody has to rediscover it:** the *critic* row is ungated as above; the
llama.cpp parse path is **unobserved** — no `llama-server` existed on the network to probe, so
`parseLlamaCppProps` is unit-tested against a shape nobody has seen; `ipc-handlers.ts` registration is
untested because no test in this repo imports that file; and `COORDINATOR_MIN_CONTEXT_TOKENS = 16384` is a
judgement, not a measurement, documented beside the constant.

**The label, and the prerequisite that made it honest.** `ModelPickerPopover` should say when the selected
model is one no engine currently reports — the retraction above records that I claimed this existed when it
did not. It could not be built on the old picker because an empty `models` list is ambiguous between
**"loaded, and the model is absent"** and **"nobody has asked yet"**, and a trigger reading "no engine
reports this model" in the second case would be a new false statement of exactly the kind being removed. So
`catalogueState: "idle" | "loading" | "loaded" | "failed"` came first — set on all eight paths that touch the
list, with a lane that *refused to answer* recorded as `failed` rather than `loaded-and-empty` — and the label
is rendered only in `loaded`. That gate was falsified: reduced to `!inCatalogue`, the chip fires in three
wrong states. Landed in `a90b8e7` with the engine-versus-file attribution (`capabilities` on a routing entry
is now a user-editable field, consulted only when the engine said nothing, and disagreement is shown naming
the engine's own report rather than silenced).

**Followed immediately by `b4d81b2`, which fixed a defect the new field introduced.** `flattenForRunStyle`
rewrote every slot onto `{ engine, model }`, quietly discarding an entry's assertion — on `single`, the one
style where the engine is typically silent and the assertion is the only statement that exists. The rule
applied is narrower than "keep the field": an assertion describes one *(engine, model)* pair, so it travels
when the pin **is** the model `default` names, and is dropped entirely when the lane is pinned to a
different model, because copying the claim across would assert something nobody said. Proven in both
directions; removing the carry fails the first test and passes the second.

**Also learned while building it, and it invalidates one of my own briefs:** I asserted the label would fire
constantly in `SetupWizard.test.tsx` because the store default `qwen3:14b` isn't pulled on a clean box.
Wrong — `SetupWizard` never renders `ModelPickerPopover`; only `InputBar` does. A premise written into an
instruction is still a claim, and this one was unverified.

## Corrections to the sections above, verified against the committed tree

- **C3 is already done as configuration.** `package.json` carries a `mac` block (`dmg`, `x64` + `arm64`)
  and a `linux` block (`AppImage`, `x64`) next to `win`/`nsis` — the claim that "no `mac` or `linux`
  block yet" is false. What is actually missing is a build: `dmg` for macOS cannot be produced or
  notarised from this Windows host, so the mac lane needs a macOS runner (or an explicit deferral), and
  the honest remaining work is "does it build and install", not "add the stanza".
- **Phase A's verification no longer gates on Memex Anywhere** (see decision 1).
- Repo hygiene, same class as the CAD-scratch rule: the tracked root `main.js` is a vestigial bundle —
  `package.json` `"main"` is `dist-electron/main.js` and `build.files` ships `dist/**` and
  `dist-electron/**`. Root `main.js` was committed once and has never been refreshed; propose
  `git rm --cached main.js` plus an ignore rule.
