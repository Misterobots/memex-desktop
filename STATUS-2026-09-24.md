# Status report — 2026-09-24, against `plan-09-24-2026.md`

Scope of this file: the day's work, mapped item-by-item to the governing plan. It does not restate the
plan's reasoning; it says what is true now, how it was verified, and what is left. Plan:
`plan-09-24-2026.md`. Records: `MEMEX_CODE_QOL_PLAN.md`, `MEMEX_CORE_HANDOFF.md`, `IMPLEMENTATION_LOG.md`.

Repo: `memex-desktop`, branch `fix/pioneers`, HEAD `3efe4a5`, working tree clean (updated 2026-09-25).
Suite: typecheck clean, **45 files / 413 tests**. Last version bump commit is `2beaf7b` (`0.1.113`); **no
build has been run since**, so everything below is source + tests, not a qualified release, and whether
`0.1.113` is the installed app on this machine is unconfirmed.

Status vocabulary: **LANDED-e2e** (behaviour observed in a running app) · **LANDED-unit** (typecheck +
unit/DOM only) · **PARTIAL** · **NOT STARTED** · **BLOCKED** (says on whom) · **DECIDED** (owner call,
unbuilt) · **UNVERIFIED** (I cannot see it from this repo).

## Owner decisions (plan: "Desktop-side realignment")

| # | Decision | Status |
| --- | --- | --- |
| 1 | No more Memex Anywhere; the site is landing + admin only | **LANDED-unit** `729f270` |
| 2 | Desktop owns model routing; `config.json` is authoritative both ways | **LANDED-e2e** `9f9bfa7` |
| 3 | Guided setup keyed on run style: `single` pinned vs `multi` swapping | **LANDED-unit** `83ec632`, `f3ced0a` |
| 4 | Desktop controls Team Builder's per-role map | **LANDED-unit** — assignment built in the app `068edd8`; the binding half landed 2026-09-25 (`Memex_Core 50e5ca0` + app `3efe4a5`), so the app's routing table now reaches a coordination run. It was never blocked on another owner: that checkout is app work, and the claim it was is retracted |
| 5 | Orchestrator placement is an output of the chosen scenario, not a choice | **DECIDED** — schema for `lane→GPU` not built |

## Phase A — website split (`Memex_Core/ui`) — all UNVERIFIED from here

The daemon guard blocks git outside this checkout, so this phase is reported from the owner's pasted
`git log`, not observed by me.

| Item | Status | Note |
| --- | --- | --- |
| Prune product surface (swarm/dev/tools/routines/research/art-studio/training/control/palace + media pages, chat/dev/palace/swarm/training/tools component trees) | **UNVERIFIED — appears committed** as `f5bbcc5` (~146 files) | Deletions and the `model-selector`/`theme-*` renames were staged before the split; the split itself was correct (test commit `e0e3e0f` first, ui commit second) |
| Nav entries removed with the pages (`navigation.ts`, `sidebar.tsx`, `client-shell.tsx`, `globals.css`, `layout.tsx`, `page.tsx`, `settings/page.tsx`) | **UNVERIFIED** | These were *unstaged* when the split happened. If they were not staged into `f5bbcc5`, that commit deletes routes while nav still points at them |
| New landing/download page (`ui/src/app/download/`) | **UNVERIFIED** | It was listed untracked (`??`) in the paste taken *before* `git add -A ui/`, so it may or may not be inside `f5bbcc5` |
| `api/backend/[...path]` kept "because Memex Anywhere needs it" | **SUPERSEDED** | Decision 1 deleted the dependency. Keeping it is now a Phase A question about the site's own admin pages, not a desktop requirement |
| Phase A verification: `cd ui; npm run build` + a health check through `/api/backend/` | **NOT PERFORMED** | The build is the item that matters — it is the only check that the prune left the site compiling. The `/api/backend/` curl is no longer a desktop acceptance check |
| Was `Memex_Core` pushed? | **UNKNOWN** | Never confirmed either way |

To close this phase's uncertainty, in `C:\Users\panca\Documents\Github\Memex_Core`:

```powershell
git log --oneline -3
git status --short
git show --stat --oneline f5bbcc5 -- ui/src/app/download ui/src/lib/config/navigation.ts
cd ui; npm run build
```

## Phase B — desktop covers what the site loses

**NOT STARTED.** This was specified as a verification pass, not construction: before the website's
`art-studio`, `training`, `palace`, `tools`, `routines` and `research` pages go away permanently, confirm the
desktop has an equivalent surface for each and file any real gap separately. Nothing has been checked
feature-by-feature. Today's work touches the shared ones (routines, research/collective, memory) but nobody
has compared the two lists.

## Phase C — guided local infrastructure

| Item | Status | What's left |
| --- | --- | --- |
| C1 engine choice + run style (Ollama vs llama.cpp, single vs multi, no silent default) | **LANDED-unit** `83ec632`, `f3ced0a`, `068edd8` | Never seen rendered. `single` writes the pin; multi writes role rows |
| C2 per-OS installers (`winget` / `brew` / official script; llama.cpp prebuilt releases, no Docker prerequisite) | **NOT STARTED** | D3b. The wizard still only links out to ollama.com — and now says so rather than implying it installs |
| C3 multi-platform packaging | **PARTIAL, plan corrected** | `package.json` already had `mac` (dmg x64+arm64) and `linux` (AppImage) blocks — the plan's "no mac or linux block yet" was false. What's missing is a build: a dmg cannot be produced or notarised from this Windows host, so it needs a macOS runner or an explicit deferral |
| Existing scaffolding the plan credits (`SetupWizard.tsx`, `inspectLocalLlm`, `recommendLocalModels`) | **EXTENDED** | `local-llm.ts`'s house default **was** dropped by `83ec632`. The LAN addresses survive elsewhere, all verified by grep: `config-store.ts:124-126,138` (seed profiles), `src/lib/desktop.ts:175` (`MEMPALACE_DEFAULT`, still imported by `runtime-urls.ts`), and `ipc-handlers.ts:684,708,753` |

## D-series (desktop-side detail in the plan)

| Item | Status | Evidence / what's left |
| --- | --- | --- |
| D0 Memex Anywhere removal | **LANDED-unit** `729f270` | 39/182 at the time; migration tested. **Not** seen in a running app |
| D1a engine registry | **LANDED-unit** `befc4b8` | Ollama `/api/tags` confirmed against a live daemon (13 models) |
| D1b picker reads the registry | **LANDED-unit** `befc4b8` | Acceptance test exists: runtime hostile (every fetch rejects), list still renders |
| D1c role map on the wire | **LANDED-unit** (map) / **NOT STARTED** (`engine`) | Both halves landed 2026-09-25: runtime `Memex_Core 50e5ca0` (`ChatRequest.role_models` → ctx → `coordinate_task` → `CoordinatorSession` → `snapshot_role_models`, third source `desktop` surviving `from_dict`, dead `ctx["team_builder_roles"]` read removed) and app `3efe4a5` (`runRoleMap` → `routing:runMap` → `role_models`, sparse by design). **`engine` is still not sent** — the map carries model ids, and which lane hosts them remains the runtime's decision. Never round-tripped against a live server |
| D3c desktop stops emitting placeholders in `model` | **LANDED-unit** `06d86b8` | `streamChat` refuses a turn with no resolved model instead of substituting `"swarm"`; the four call sites are gone, `SteeringCard` was the load-bearing one. Three existing tests had been relying on the substitution and now name a model like every real caller. No packaged/native check |
| D2 routing table | **LANDED-e2e** `9f9bfa7` | A pick survived quit + relaunch on the owner's install — the day's only end-to-end acceptance |
| D3a discovery + run style | **LANDED-unit** `83ec632` | AdapterRAM defect fixed and measured in `0dbdec0` (two 5060 Ti read 15.9 GB each, 31.8 total) |
| D3a-2 role assignment | **LANDED-unit** `068edd8` | Seven roles, `multi` only; `critic` excluded on purpose; `engines:modelsFor` is id-only so main never probes a renderer-supplied URL |
| D4 capability matrix | **LANDED-unit** `37b83bd`, `a90b8e7`, `b4d81b2` | Driven by `/api/show` `capabilities`. Found and fixed: `nomic-embed-text` (no `completion`) was offered as a chat model |
| D4 gate: critic / structured output | **NOT STARTED** | Nothing in `/api/show` reports parseable structured output; substituting `thinking` would be a guess. Needs a real signal or an explicit assertion |
| `lane→GPU` config field (decision 5's multi-GPU scenario) | **NOT STARTED** | `engines.<id>` carries `kind`/`baseUrl`/`label`/`pinnedModel` — nothing expresses which card a lane owns |
| Managed local runtime (install + supervise, no Docker prerequisite) | **NOT STARTED** | The largest unbuilt item left. Nothing in `electron/` starts or watches a process beyond terminals, LSPs and OpenSCAD |
| Routines per-placement copy | **PARTIAL** `ed1987b` | The screen now names the runtime that owns the schedule and states the truth for today — they fire on that host's clock, whether or not the desktop is open. The `managed` wording waits for a supervisor that can honour it; placement is read from the active profile rather than stored, so the config never carries a claim no code enforces |
| Repo hygiene: `SEED_PROFILES` house IPs, `MEMPALACE_DEFAULT`, stale tracked root `main.js`, 43 Dependabot findings | **NOT STARTED** | Owner has not chosen; none of it is blocking |

## Known gaps in the evidence, stated plainly

1. **No native acceptance for five UI slices.** D0, D3a, D3a-2, D4 and the capability assertions are
   typecheck + vitest/jsdom only. `AGENTS.md` requires the packaged desktop build for desktop-specific
   acceptance; step 0 is now tall enough to need a real window at a real scale.
2. **llama.cpp discovery and props parsing are unobserved.** No `llama-server` answered on `:8080/8081/8000/1234`
   on this machine, Turing, or the LAN host, so that path is tested against a shape nobody has seen.
3. **The IPC layer is untested.** No test in this repo imports `ipc-handlers.ts`; the capability and
   candidate channels are proven on the pure functions they delegate to, not on the registration.
4. **`COORDINATOR_MIN_CONTEXT_TOKENS = 16384` is a judgement**, documented beside the constant, not a measurement.
5. **Two agents and I produced false reports today.** One claimed a committed slice with an invented hash and
   invented symbols; one claimed to have saved a memory file it never wrote; and I twice wrote commit hashes
   that did not exist and once called a local commit "pushed". All were caught by `git log`, `grep` and a real
   test run — which is now the standing protocol: agents never commit, and every claim is checked against the
   tree before it is repeated.
6. **One of my own plan claims was wrong and is corrected in place**: routines do not depend on the app being
   open today. The retraction and its reasoning are in `plan-09-24-2026.md`.

## What's next, in the order I'd take it

1. **Native pass** — `npm run build:electron && npm start`, then Settings → *Set up Local LLMs…*: step 0 fit
   and scroll, step 1's role rows and capability marks, the disagreement notice, and the "no engine reports
   this model" chip firing only when the catalogue genuinely loaded.
2. **D3c (desktop-only, mine to do)** — stop the desktop emitting placeholders in `model`. **Four** sites
   substitute the sentinel, all verified by grep: `sse-stream.ts:146` and `:170`, `InputBar.tsx:409` and
   `:443`. Each becomes the routing table's resolved default, and an unresolvable model is a stated error
   rather than a fallback string — omitting the field is not a fix, because `ChatRequest.model` defaults to
   `"default"` (`main.py:654`), which `coordinate.py:131` would also read as a model id. Verified safe to do
   alone: `_swarm_turn` (`main.py:2451-2462`) ORs `swarm_mode`, so the sentinel does no routing work for this
   client — every request that ever carried it also carried the flag.
3. **D1c — landed as source; one acceptance left.** Runtime `Memex_Core 50e5ca0`, app `3efe4a5`. The dead
   `ctx["team_builder_roles"]` read is gone and the map arrives as `role_models`. The placeholder guard this
   item used to call for turned out to already exist — `church.py` nulls any `model` with no `:` before a
   handler sees it — so nothing was added to `coordinate.py`, which is the correction I owe that line.
   **Remaining before this reads as LANDED-e2e:** send one real turn with two roles assigned, then read that
   run's `00_role_model_snapshot_*.json` and confirm `source: "desktop"` with those two bindings. Needs the
   orchestrator running. `engine` on the wire stays open.
4. **Phase A verification in `Memex_Core`** — the `ui` build is the one check that says whether the prune left
   the site compiling.
5. **Phase B** — the desktop-covers-the-site feature comparison, before the removed pages are gone for good.
6. **Then the two big builds** — `lane→GPU` schema, then the managed local runtime and D3b installers.
